// LEGACY SCREEN REMOVED
// This file intentionally reduced to a stub for backward compatibility.
// Any attempt to use this widget should surface a clear error. Safe to delete
// once all references to ModernBlogListScreen are gone.

import 'package:flutter/widgets.dart';

class ModernBlogListScreen extends StatelessWidget {
  const ModernBlogListScreen({super.key});

  @override
  Widget build(BuildContext context) => throw UnsupportedError(
        'ModernBlogListScreen has been removed. Use BlogListScreen instead.',
      );
}